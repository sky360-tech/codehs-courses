function start(){
    var name = readLine("Name? ");
    println("Your name is: " + name );
    var numApples = readInt("Number of apples? ");
    println(numApples + " apples");
    var numOranges = readInt("Number of oranges? ");
    println(numOranges + " oranges" );
	
}