In the `CarUtility` class, what are the 
parameters for the `driveCar` method? 


In the `CarUtility` class, what are the 
parameters for the `refuelCar` method?


How do the `driveCar` and `refuelCar` methods 
access the attributes of the `car` object? Why 
do they use this approach?


In the `CarRunner` file, describe what happens 
when line 10 executes: `CarUtility.driveCar(myCar, 50);`.